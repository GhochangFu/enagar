# Optional overrides — by default `next.config.mjs` loads `../../infrastructure/.env`
# and mirrors ALLOW_CLIENT_SCAN_SIMULATION → NEXT_PUBLIC_ALLOW_CLIENT_SCAN_SIMULATION.

# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api

# Local document scan speed (must match API ALLOW_CLIENT_SCAN_SIMULATION=true):
# NEXT_PUBLIC_ALLOW_CLIENT_SCAN_SIMULATION=true
