/** Portal tenant for citizen OTP (Option A — matches `apps/citizen-pwa`). */
export const CITIZEN_PORTAL_TENANT_CODE = 'WBPORTAL';

/** Maximum pinned municipal ULBs (`PATCH /citizen/preferences`). */
export const MAX_PINNED_MUNICIPALITIES = 15;
