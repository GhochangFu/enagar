{
  "name": "@enagar/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Citizen mobile app (React Native + Expo). Phase 5/8 (per ADR-0003 PWA-first lead).",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "echo '@enagar/mobile: Phase-5 stub — see ADR-0003 + ROADMAP.md'",
    "build": "echo 'noop (stub)'",
    "lint": "echo 'noop (stub)'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "clean": "rimraf .expo dist .turbo"
  },
  "dependencies": {
    "@enagar/types": "workspace:*"
  },
  "devDependencies": {
    "@enagar/config": "workspace:*",
    "@types/node": "^20.16.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
