# @enagar/admin-tenant — STUB (Phase 6)

Per-municipality administrative portal for **Tenant Admins**:

- Service catalogue: enable/disable, fee structures, eligibility, workflow stages
- Form designer (JSON-Schema, drag-and-drop)
- Branding: logo, colour, language priority
- Operator/staff role management
- Per-tenant feature flags (chatbot provider, EV charging, parking, etc.)

## Status

Phase-0 stub only — `package.json` + `tsconfig.json` + this README. Full Next.js scaffold lands in Phase 6 (Sprint 6.1) per `ROADMAP.md`. Established patterns from `apps/citizen-pwa` will be reused.
