/** Roles permitted to export finance reconciliation artefacts (ADR-006 / roadmap Phase 3). */
export const RECONCILIATION_EXPORT_ROLES = new Set([
  'tenant_admin',
  'municipality_admin',
  'state_admin',
]);
