/** Staff roles permitted to operate Sprint 3.3A finance endpoints (deposit / refund approvals / challans). */
export const FINANCE_APPROVAL_ROLES = new Set([
  'tenant_admin',
  'municipality_admin',
  'state_admin',
]);
