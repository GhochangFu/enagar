{
  "name": "@enagar/grievance-catalogue",
  "version": "0.0.0",
  "private": true,
  "description": "Shared grievance taxonomy catalogue types, fetch helpers, and label resolution for citizen clients.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "build": "tsc",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@enagar/i18n": "workspace:*"
  },
  "devDependencies": {
    "@enagar/config": "workspace:*",
    "eslint": "^10.5.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
