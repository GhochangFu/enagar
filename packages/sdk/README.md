# @enagar/sdk — STUB (Phase 1)

Typed API client consumed by every UI app. **Generated** from the OpenAPI spec exported by `apps/api` (NestJS Swagger module) so that contract drift is impossible.

## Status

Phase-0 stub. Code-generation pipeline is wired up in Sprint 1.1; until then this package only re-exports `@enagar/types`. Pact contract tests live in `tests/contract` (Phase 1).
