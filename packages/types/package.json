{
  "name": "@enagar/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain types — Tenant, Citizen, Service, Application, Grievance, Payment, LLM provider, etc.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./llm": "./src/llm.ts",
    "./tenant": "./src/tenant.ts",
    "./setup-assistant-flow": "./src/setup-assistant-flow.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "build": "tsc",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@enagar/config": "workspace:*",
    "eslint": "^10.6.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
