// Phase-0 placeholder. Phase 2 lands the JSON-Schema → React/RN renderer
// (RJSF on web; custom RN-friendly renderer on mobile) plus Ajv-based
// validators shared between client and server.
export const formsPlaceholder = '@enagar/forms — Phase 2';
