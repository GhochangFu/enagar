{
  "name": "@enagar/smart-city-adapters",
  "version": "0.0.0",
  "private": true,
  "description": "Smart-city sensor and meter adapter interfaces with stub implementations.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test test/run-tests.mjs",
    "build": "tsc",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@enagar/config": "workspace:*",
    "eslint": "^8.57.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
