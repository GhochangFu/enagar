// Phase-0 placeholder. Phase 5 introduces the RN counterparts of the
// @enagar/ui primitives. The shape of the public API mirrors @enagar/ui
// 1:1 so feature code can sometimes share code via re-exports.
export const uiNativePlaceholder = '@enagar/ui-native — Phase 5';
