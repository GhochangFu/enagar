# @enagar/ui-native — STUB (Phase 5)

React Native UI primitives. Public API mirrors `@enagar/ui` so feature code can stay close in shape across web and mobile.

## Status

Phase-0 stub. Implemented in Phase 5 alongside `apps/mobile`.
