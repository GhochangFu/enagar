# @enagar/workflow — STUB (Phase 2)

Workflow / state-machine **types** shared between the workflow runtime (`services/workflow-engine`), the admin designer (`apps/admin-tenant`), and the API. Defines stages, transitions, guards, side-effects, and SLA policies.

## Status

Phase-0 stub. Implemented in Phase 2 once the form runtime exists (workflows reference form schemas).
