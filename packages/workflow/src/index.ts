// Phase-0 placeholder. Phase 2 lands the workflow definition types
// (stage, transition, condition, action, sla) used by both
// services/workflow-engine (BullMQ-driven runner) and the
// admin-tenant workflow designer.
export const workflowPlaceholder = '@enagar/workflow — Phase 2';
