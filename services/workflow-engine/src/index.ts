// Phase-0 placeholder. Phase 2 introduces the actual BullMQ worker that
// drives stage transitions, dispatches notifications, and arms SLA timers.
export const workflowEnginePlaceholder = '@enagar/workflow-engine — Phase 2';
