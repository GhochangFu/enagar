{
  "name": "@enagar/reporting-worker",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ worker — scheduled reports, materialized-view refreshes, exports. Phase 6.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "echo '@enagar/reporting-worker: Phase-6 stub'",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@enagar/types": "workspace:*"
  },
  "devDependencies": {
    "@enagar/config": "workspace:*",
    "@types/node": "^20.16.10",
    "eslint": "^10.4.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
