# SSA-2 (EN-58) — Service Setup Assistant Form Step Implementation Plan

**Status:** **Done** (2026-06-22) · Jira [EN-58](https://ghochangfu.atlassian.net/browse/EN-58) · Merged to `main` · Commits `4df57ea`, `f301339`

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Deliver the Form step for Service Setup Assistant — typed LLM tools for tenant and state admin, SSE chat orchestration, form preview UI, and auto-save drafts via existing designer APIs.

**Architecture:** Extend `service-setup-assistant` with a per-step tool registry, tenant/state form tools calling `AdminTenantService` / `AdminStateService`, and `ServiceSetupAssistantService` streaming SSE via reused `ChatbotLlmService`.

**Tech Stack:** NestJS, Prisma, Next.js 14, `@enagar/forms`, existing chatbot LLM adapter (ADR-0008), Jest.

**Jira:** [EN-58](https://ghochangfu.atlassian.net/browse/EN-58) · Parent [EN-51](https://ghochangfu.atlassian.net/browse/EN-51)

**Depends on:** SSA-1 (EN-53) — sessions, readiness, wizard shell.

---

## Deliverables

| ID  | Deliverable                              | Files                                                                               |
| --- | ---------------------------------------- | ----------------------------------------------------------------------------------- |
| D1  | Tool registry foundation                 | `tools/tool.types.ts`, `tools/tool-registry.ts`                                     |
| D2  | Tenant form tools                        | `tools/tenant-form.tools.ts`, `tenant-form.tools.spec.ts`                           |
| D3  | State global form tools + routes         | `tools/state-global-form.tools.ts`, `state-form-assistant.controller.ts`            |
| D4  | LLM orchestration + SSE message endpoint | `service-setup-assistant.service.ts`, `prompts/*.md`, controller `POST .../message` |
| D5  | Message/audit persistence                | Uses existing `service_setup_messages`, `service_setup_audit_logs`                  |
| D6  | Tenant form step UI                      | `setup-assistant-client.tsx` — chat SSE, form preview, step completion              |
| D7  | State form wizard UI                     | `admin-state/.../setup-assistant/*`, link from `global-form-builder-client.tsx`     |
| D8  | Tests + verification evidence            | Unit/integration specs, typecheck logs                                              |

---

## Exit Criteria

SSA-2 is complete when all are true:

1. **Tool registry:** `getToolsForStep(persona, scope, step)` returns only allowed tools for form step (step 2).
2. **Tenant tools:** `applyFormDraft`, `loadGlobalTemplate`, `proposeFormFields` validate before write; wrong `service_code` rejected.
3. **State tools:** `applyGlobalFormSchema`, `proposeGlobalFormFields` validate and patch global template; `state_admin` guard enforced.
4. **SSE:** `POST .../message` streams `meta`, `token`, `tool_result`, `draft_updated`, `done`, `error` events per `SetupAssistantSseEvent`.
5. **Persistence:** User/assistant messages and tool audit rows written per turn.
6. **DPA:** Reuses `ChatbotLlmService` with `SETUP_ASSISTANT_SKIP_DPA_DEV` for local dev (ADR-0016).
7. **UI tenant:** Form-only scope E2E — chat sends message, preview refreshes on `draft_updated`, step 2 marked complete when `form_draft_valid` is green.
8. **UI state:** Form assistant shell at `/dashboard/library/[code]/form/setup-assistant` with chat + preview.
9. **Verification:** New API specs pass; mocked LLM integration test green.
10. **Review:** Spec compliance + code quality review with no open high-severity issues.

---

## Implementation Tasks

### Task 1 — Tool registry

- [x] `SetupToolContext`, `SetupToolDefinition`, `SetupToolResult` types
- [x] `getToolsForStep('tenant' | 'state', scope, step)` — form step tools only in SSA-2
- [x] `executeTool(name, ctx, args)` with allowlist enforcement

### Task 2 — Tenant form tools (TDD)

- [x] `applyFormDraft` → `AdminTenantService.saveFormDraft`
- [x] `loadGlobalTemplate` → resolve global schema (read-only preview)
- [x] `proposeFormFields` → merge/normalize fields, auto-save valid drafts, support field relocation
- [x] Unit tests: invalid schema, valid persist, service_code mismatch

### Task 3 — State global form tools

- [x] `AdminStateService.patchGlobalFormSchema` (validate + update `formSchema`)
- [x] `applyGlobalFormSchema`, `proposeGlobalFormFields` tools
- [x] `StateFormAssistantController` — sessions + readiness + message SSE under `admin/state/global-service-library/:code/setup-assistant/*`
- [x] Export `AdminStateService` from `AdminStateModule`

### Task 4 — Session extensions

- [x] `createStateFormSession(globalServiceCode)` — scope `form`, step 2
- [x] `markStepComplete(sessionId, stepKey)` — update `step_completion` JSON
- [x] State session access asserts `globalServiceCode` matches route

### Task 5 — LLM orchestration (TDD)

- [x] `ServiceSetupAssistantService.streamTenantMessage` / `streamStateMessage`
- [x] System prompts (`prompts/system-tenant-form.md`, `prompts/system-state-form.md`)
- [x] Parse `tool_calls` JSON block from assistant text; execute server-side
- [x] Emit `draft_updated` on mutating tools; audit log per tool call
- [x] `ChatbotLlmService` DPA context for setup assistant (`SETUP_ASSISTANT_SKIP_DPA_DEV`)
- [x] Integration test with mocked provider returning tool call JSON

### Task 6 — Tenant UI

- [x] SSE client (`lib/setup-assistant-sse.ts`)
- [x] Chat panel: message list, input, streaming tokens
- [x] Form preview via `GET .../designer` + `FormCitizenPreview`; refresh on `draft_updated`
- [x] Auto-mark step 2 complete when readiness `form_draft_valid` is green

### Task 7 — State UI

- [x] `setup-assistant/page.tsx` + `state-form-assistant-client.tsx`
- [x] Link from global form builder header
- [x] Same chat + preview pattern against state global template API

---

## Automated Testing Plan

### API / backend

```bash
pnpm --filter @enagar/api test -- tenant-form.tools.spec tool-registry.spec service-setup-assistant.service.spec service-setup-assistant.controller.spec state-form-assistant.controller.spec
pnpm --filter @enagar/api typecheck
```

### Frontend

```bash
pnpm --filter @enagar/admin-tenant typecheck
pnpm --filter @enagar/admin-state typecheck
```

### Migration

No new migration in SSA-2 (uses SSA-1 tables).

---

## Code Review Gates

### R1 — Spec compliance

- [x] Form step only — no workflow/payment tools exposed
- [x] No publish/auto-publish tools in registry
- [x] SSE contract matches `SetupAssistantSseEvent`
- [x] State admin limited to form scope

### R2 — Code quality

- [x] Tool execution never trusts client-supplied writes without validation
- [x] Tenant isolation on sessions and tools
- [x] Tests cover failure paths
- [x] Minimal diff outside SSA module + UI shells

### R3 — Verification evidence

- [x] Command outputs captured before completion claim
- [x] PR lists automated + manual test status

---

## Manual UI Tests (Your side)

**Prerequisites:** `pnpm --filter @enagar/api prisma:migrate:deploy`, API + admin apps running, `SETUP_ASSISTANT_SKIP_DPA_DEV=true` in dev, LLM provider configured (`LLM_PROVIDER` / Ollama local).

### Tenant (`admin-tenant`)

1. Open Setup Assistant → **Form only** scope → confirm step 2 active.
2. Send chat message: _"Add a text field called applicant name"_ — verify streaming reply and tool result in UI.
3. Confirm form preview updates after `draft_updated` without manual refresh.
4. Readiness sidebar: `form_draft_valid` turns green after valid draft applied.
5. Reload page with `?sessionId=` — chat history loads (if history endpoint used) and preview shows saved draft.
6. Open Service Designer — confirm form draft matches assistant changes.

### State (`admin-state`)

7. Open Global Form Builder → **Open Setup Assistant** link.
8. Create form session; send message to add a field; verify global template preview updates.
9. Return to Global Form Builder — saved draft reflects assistant changes.

### Security

10. Tampered `sessionId` or cross-tenant service ID → request rejected.
11. Non-`state_admin` token on state routes → 403.

---

## PR Requirements

- Summary of D1–D8 deliverables
- EN-58 reference
- Test evidence block
- Manual UI checklist with `done` / `pending` per item

---

## Completion record

| Field     | Value                                                                                    |
| --------- | ---------------------------------------------------------------------------------------- |
| Completed | 2026-06-22                                                                               |
| Branch    | `feat/en-58-ssa-2` → `main`                                                              |
| Commits   | `4df57ea` feat(api): EN-58 SSA-2 form step; `f301339` fix(api): form assistant hardening |
| Jira      | EN-58 → Done                                                                             |
| Tests     | 26 SSA-related API specs passing at completion                                           |
