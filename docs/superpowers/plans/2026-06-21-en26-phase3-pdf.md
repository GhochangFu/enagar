# EN-26 Phase 3 — PDF import Implementation Plan

> **Goal:** Add PDF import with AcroForm, digital-text heuristics, handwritten rejection, and basic OCR (EN-39–EN-42).

**Architecture:** `extractFormImportProposalFromPdf` tries AcroForm → digital text → OCR → EN-41 handwritten gate. Shared layout heuristics from text lines. UI accepts `.pdf`.

**Tech Stack:** pdf-lib, pdf-parse@1.1.1, pdf-to-png-converter, tesseract.js

---

See shipped code under `apps/api/src/modules/form-import/extractors/pdf-*.ts` and `docs/runbooks/form-import-pdf.md`.
